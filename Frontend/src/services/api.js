export const API_BASE_URL = 'http://localhost:8080/api/users'; // Update to your local backend URL

export const ENDPOINTS = {
  LOGIN: `${API_BASE_URL}/login`,
  REGISTER: `${API_BASE_URL}/signup`,
}